.group-content
  - current_user.groups.each do |group|
    = link_to group_messages_path(group) do
      .group-content__group-name
        = group.name
      .group-content__group-last-message
        メッセージはまだありません。
